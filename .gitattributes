# Normaliza los finales de linea en el repositorio y usa CRLF al descargar en Windows.
# Los .ps1 van en UTF-8 con BOM: git no toca el BOM, pero no los guardes sin el.
* text=auto
*.ps1 text eol=crlf
*.vbs text eol=crlf
*.md  text eol=crlf

*.png binary
*.jpg binary
*.ico binary
